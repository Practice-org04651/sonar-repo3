name: Build

on:
  push:
    branches:
      - master
      - Dev

jobs:
  sonarqube:
    name: Sonarscan
    name:sonarscan
    uses: SonarSource/sonarqube-scan-action@v6
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
