name: Build

on:
  push:
    branches:
      - master
      - Dev

jobs:
  sonarqube:
    name: sonar
    name: sonar
    name: Sonarscan
    uses: path/commonfile
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
